version: '3.8'

services:
  newsdrop:
    build: .
    container_name: newsdrop-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
